        MODULE directory
        CHARACTER(LEN=13),PARAMETER   :: session_dir = 'session_param'! directory for session input and parameters
        CHARACTER(LEN=13),PARAMETER   :: saved_session='saved_session'! directory to print out session input and parameters
        CHARACTER(LEN=11),PARAMETER   :: gupta_dir='param_Gupta'      ! Gupta parameter directory
        END MODULE directory
